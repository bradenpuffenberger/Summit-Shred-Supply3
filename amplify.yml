version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "Building static site"
        - cp homePage.html index.html
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []

version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "Build phase - copying static files"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
